# Extra packages for the 3D brain bake (python/extract_3d.py, python/bake_3d.py); the model itself needs only requirements.txt.
# Install on top of it:  .venv/Scripts/python -m pip install -r requirements.txt -r requirements-3d.txt
cloud-volume==12.15.0
DracoPy==1.7.0
fast-simplification==0.2.0
trimesh==5.1.0
navis==1.12.0
flybrains==0.6.3
