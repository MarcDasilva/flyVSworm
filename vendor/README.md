# Local clone of https://github.com/XuNeo/luavgl
#
# HTN has not published the Hacker Badge firmware. This is the public
# Lua + LVGL binding used by similar ESP32 Lua UIs. Gitignored; refresh with:
#
#   git clone --depth 1 https://github.com/XuNeo/luavgl.git vendor/luavgl
#
# The API your app may actually call is docs/badge-ide-README.md, not raw LVGL.
